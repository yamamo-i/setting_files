unbind C-b
set -g prefix C-t
bind C-t send-prefix

#set -g default-terminal "xterm-256color"
#set -g terminal-overrides "term:colors=256"
#set -g display-time 2000

bind H split-window -v

bind v split-window -h

bind k kill-pane
bind K kill-window

# default statusbar colors
#set-option -g status-bg colour235 #base02
set-option -g status-fg colour136 #yellow
set-option -g status-attr default


# clock
set-window-option -g clock-mode-colour colour64 #green


# mouse
set-option -gw mode-mouse on
set-option -g mouse-utf8 on
set-option -g mouse-resize-pane on
set-option -g mouse-select-pane on
set-option -g mouse-select-window on
set-window-option -g mode-keys vi

# ==== Shared clipboard
#bind-key C-q save-buffer ~/.tmux-buffer \; run-shell 'xsel -b -i < ~/.tmux-buffer' \; display-message "Copied to clipboard."
#bind-key C-p if-shell 'xsel -b -o > ~/.tmux-buffer' 'load-buffer ~/.tmux-buffer ; paste-buffer'

